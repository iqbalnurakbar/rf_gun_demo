<mvc:View
    xmlns:layout="sap.ui.layout"
    controllerName="rfgundemo.controller.DataDetail"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    displayBlock="true"
>
    <Page
        id="dataDetailPage"
        title="SMR Demo Apps"
        showNavButton="true"
        navButtonPress=".onNavBack"
        class="sapUiResponsiveContentPadding"
    >
        <List
            id="orderList"
            mode="MultiSelect"
        >
            <headerToolbar>
                <Toolbar>
                    <Title id="orderDetailsTitle" />
                    <ToolbarSpacer />
                    <Button
                        id="saveAndPostButton"
                        icon="sap-icon://save"
                        press=".onSaveAndPostButtonPress"
                        text="Save and Post"
                        type="Emphasized"
                    />
                </Toolbar>
            </headerToolbar>
            <CustomListItem id="orderListItem">
                <HBox>
                    <VBox class="sapUiSmallMargin" width="3rem">
                        <Label
                            text="No."
                            labelFor="purchaseOrderItemNoInput"
                        />
                        <Input
                            id="purchaseOrderItemNoInput"
                            value="{PurchaseOrderItemNo}"
                            editable="false"
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin" width="20rem">
                        <Label
                            text="Material"
                            labelFor="materialInput"
                        />
                        <Input
                            id="materialInput"
                            value="{Material}"
                            editable="false"
                            visible="false"
                        />
                        <Input
                            id="materialInputShow"
                            value="{Material} {MaterialDescription}"
                            editable="false"
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin" visible="false">
                        <Label
                            text="Material Desc"
                            labelFor="materialDescriptionInput"
                        />
                        <Input
                            id="materialDescriptionInput"
                            value="{MaterialDescription}"
                            editable="false"
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin">
                        <Label
                            text="Quantity Suggest"
                            labelFor="quantitySuggestInput"
                        />
                        <Input
                            id="quantitySuggestInput"
                            value="{QuantitySuggest}"
                            editable="false"
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin">
                        <Label
                            text="Quantity Receive"
                            labelFor="quantityReceiveInput"
                        />
                        <Input
                            id="quantityReceiveInput"
                            value=""
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin" width="3rem">
                        <Label
                            text="Unit"
                            labelFor="unit"
                        />
                        <Input
                            id="unit"
                            value="{PurchaseOrderQuantityUnit}"
                            editable="false"
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin" width="7rem">
                        <Label
                            text="Plant"
                            labelFor="plantInput"
                        />
                        <Input
                            id="plantInput"
                            value="{Plant}"
                            showValueHelp="true"
                            valueHelpRequest="onPlantVHRequest"
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin" width="7rem">
                        <Label
                            text="Storage Location"
                            labelFor="storageLocationInput"
                        />
                        <Input
                            id="storageLocationInput"
                            value="{StorageLocation}"
                            showValueHelp="true"
                            valueHelpRequest="onStrLocVHRequest"
                        />
                    </VBox>
                    <VBox class="sapUiSmallMargin">
                        <Label
                            text="Attachment"
                            labelFor="attachment"
                        />
                        <HBox id="attachment">
                            <Button
                                class="sapUiTinyMarginEnd"
                                id="uploadButton"
                                icon="sap-icon://upload"
                            />
                            <Button
                                id="downloadButton"
                                icon="sap-icon://download"
                            />
                        </HBox>

                    </VBox>
                    <VBox class="sapUiSmallMargin">
                        <Label
                            text="Confirm Status"
                            labelFor="confirmStatusButton"
                        />
                        <ToggleButton
                            id="confirmStatusButton"
                            text="OK"
                            width="100%"
                            press="onOkButtonPress"
                        />
                    </VBox>
                </HBox>
            </CustomListItem>
        </List>
    </Page>
</mvc:View>